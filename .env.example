# Copy to .env for local development (never commit .env).
# On Hostinger, set these in hPanel → Environment variables (or a server-only .env).

DB_HOST=localhost
DB_USER=
DB_PASS=
DB_NAME=
DB_CHARSET=utf8mb4
DB_COLLATE=
APP_ENV=production
HOSTINGER_API_KEY=
