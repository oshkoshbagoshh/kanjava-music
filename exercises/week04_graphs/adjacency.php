<?php
declare(strict_types=1);

/**
 * Pattern 4 — Graphs (STUB)
 *
 * Build undirected adjacency list from collaboration pairs.
 *
 * @param list<array{0: int|string, 1: int|string}> $pairs
 * @return array<int|string, list<int|string>>
 */
function build_adjacency(array $pairs): array
{
    // TODO: for each [a, b], add b to a's list and a to b's list (undirected)
    return [];
}
