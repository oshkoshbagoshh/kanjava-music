<?php
declare(strict_types=1);

/**
 * Pattern 3 — YOUR PROJECT TASK (STUB)
 *
 * Recursively walk a project folder; identify .als, .wav, .mid, .asd;
 * return manifest entries: path, size, hash (sha256), type.
 *
 * @return list<array{path: string, size: int, hash: string, type: string}>
 */
function jaos_scan(string $root): array
{
    // TODO: recursive DirectoryIterator / scandir walk
    return [];
}
